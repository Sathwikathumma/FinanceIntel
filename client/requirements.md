## Packages
recharts | For financial scenario visualizations
framer-motion | For premium UI animations and page transitions
react-hook-form | For complex data upload forms
@hookform/resolvers | For Zod validation in forms

## Notes
The backend provides /api/user for checking auth status.
Charts will render client-side using recharts.
Ensure Tailwind configuration allows custom fonts and colors via CSS variables.
